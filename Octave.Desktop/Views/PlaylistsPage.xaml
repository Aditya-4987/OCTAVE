<Page
    x:Class="Octave_Desktop.Views.PlaylistsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:Octave.Core.Models"
    mc:Ignorable="d"
    Background="Transparent">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,24">
            <!-- Hardcoded White / #888888 / #1E1E1E replaced with theme resources -->
            <TextBlock Text="Playlists" FontSize="28" FontWeight="Bold" Foreground="{ThemeResource TextFillColorPrimaryBrush}" VerticalAlignment="Center" />
            <Button Content="New Playlist" Click="NewPlaylist_Click" HorizontalAlignment="Right" VerticalAlignment="Center" />
        </Grid>

        <TextBlock Grid.Row="1" Text="No playlists yet. Create one to get started."
                   Foreground="{ThemeResource TextFillColorTertiaryBrush}" VerticalAlignment="Top"
                   Visibility="{x:Bind EmptyVisibility(ViewModel.Items.Count), Mode=OneWay}" />

        <GridView Grid.Row="1"
                  ItemsSource="{x:Bind ViewModel.Items, Mode=OneWay}"
                  SelectionMode="None"
                  IsItemClickEnabled="True"
                  ItemClick="GridView_ItemClick">
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="models:Playlist">
                    <Grid Width="180" Height="120" Margin="8" Padding="16" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8"
                          RightTapped="PlaylistCard_RightTapped">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <FontIcon Grid.Row="0" Glyph="&#xE90B;" FontFamily="Segoe MDL2 Assets" FontSize="22"
                                  Foreground="{ThemeResource TextFillColorTertiaryBrush}" HorizontalAlignment="Left" />
                        <TextBlock Grid.Row="1" Text="{x:Bind Title}" FontWeight="SemiBold" FontSize="15" Foreground="{ThemeResource TextFillColorPrimaryBrush}"
                                   TextTrimming="CharacterEllipsis" VerticalAlignment="Bottom" />
                        <TextBlock Grid.Row="2" Text="{x:Bind TrackCount}" Foreground="{ThemeResource TextFillColorTertiaryBrush}" FontSize="12" />
                    </Grid>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
    </Grid>
</Page>
