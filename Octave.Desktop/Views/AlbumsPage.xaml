<Page
    x:Class="Octave_Desktop.Views.AlbumsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:Octave.Core.Models"
    xmlns:converters="using:Octave_Desktop.Converters"
    mc:Ignorable="d"
    Background="Transparent">

    <Page.Resources>
        <converters:ArtworkPathConverter x:Key="ArtworkPathConverter" />
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- UI-AP-04: was hardcoded White -->
        <TextBlock Text="Albums" FontSize="36" FontWeight="Bold" Margin="0,0,0,16" Padding="32,48,32,0" Foreground="{ThemeResource TextFillColorPrimaryBrush}" />

        <!-- NF-15: this GridView never declared Grid.Row, so it shared row 0 with
             the header and rendered underneath its text. Pinned to row 1. -->
        <GridView Grid.Row="1"
                  ItemsSource="{x:Bind ViewModel.Items, Mode=OneWay}"
                  SelectionMode="None"
                  IsItemClickEnabled="True"
                  ItemClick="GridView_ItemClick"
                  Padding="32,0,32,32"
                  ItemContainerStyle="{StaticResource CleanGridViewItemStyle}">
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="models:Album">
                    <!-- UI-AP-04: hardcoded #222222 / White / #AAAAAA replaced with
                         theme resources. Hover accent moved into a PointerOver
                         visual state (same treatment as HomePage, UI-HP-05). -->
                    <Grid Width="160" Height="220" Style="{StaticResource StandardCardStyle}">
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal" />
                                <VisualState x:Name="PointerOver">
                                    <Storyboard>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="BorderBrush">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource SystemControlHighlightAccentBrush}" />
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <!-- Square Artwork Image -->
                        <Border Grid.Row="0" Width="136" Height="136" Background="{ThemeResource LayerFillColorDefaultBrush}" CornerRadius="4" Margin="0,0,0,12">
                            <Image Source="{x:Bind ArtworkUrl, Converter={StaticResource ArtworkPathConverter}, ConverterParameter=Card}" Stretch="UniformToFill" />
                        </Border>

                        <!-- Title -->
                        <TextBlock Grid.Row="1" Text="{x:Bind Title}" FontWeight="Bold" FontSize="14" Foreground="{ThemeResource TextFillColorPrimaryBrush}" TextTrimming="CharacterEllipsis" />

                        <!-- Artist -->
                        <TextBlock Grid.Row="2" Text="{x:Bind ArtistName}" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextTrimming="CharacterEllipsis" Margin="0,4,0,0"/>
                    </Grid>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>

        <!-- UI-AP-01: empty state, mirroring HomePage's pattern. -->
        <Grid Grid.Row="1"
              Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
              CornerRadius="12" Padding="48" HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="500"
              Visibility="{x:Bind EmptyStateVisibility(ViewModel.Items.Count), Mode=OneWay}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="16">
                <Border Width="64" Height="64" CornerRadius="32" Background="{ThemeResource LayerFillColorDefaultBrush}" HorizontalAlignment="Center">
                    <FontIcon Glyph="&#xE93C;" FontFamily="Segoe MDL2 Assets" FontSize="28" Foreground="{ThemeResource SystemControlHighlightAccentBrush}" VerticalAlignment="Center" HorizontalAlignment="Center" />
                </Border>
                <TextBlock Text="No albums in your library" FontSize="22" FontWeight="Bold" Foreground="{ThemeResource TextFillColorPrimaryBrush}" HorizontalAlignment="Center" />
                <TextBlock Text="Scan a folder from Settings and your albums will show up here." Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" TextAlignment="Center" FontSize="14" />
            </StackPanel>
        </Grid>
    </Grid>
</Page>
